#include <iostream>
#include <cmath>
#include <vector>

using namespace std;

int main()
{
	int w,h;
	vector<int> ws, hs;
	cin>>w;
	cin>>h;
	while((w>0)&&(h>0))
	{
		ws.push_back(w);
		hs.push_back(h);
	}
	for(i=0; i<ws.size();i++)
	{
		if(h>(w*(M_PI+1)))
			//solution 1
		else
			//solution 2
	}
	return 0;
}
